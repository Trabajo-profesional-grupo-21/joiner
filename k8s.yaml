apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: joiner
  name: joiner
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: joiner
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: joiner
    spec:
      containers:
      - image: <IMAGE> # Replaced in CI
        imagePullPolicy: IfNotPresent
        name: joiner
        resources: 
          requests:
            cpu: 150m
            memory: 200Mi
          limits:
            cpu: 200m
            memory: 500Mi